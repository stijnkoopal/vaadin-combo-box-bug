# Bug in vaadin-combo-box

* Run `yarn`
* Run `yarn dev`
* Run `open http://localhost:8000` (Should open in Chrome)
* Type only `t` in the text field
* See that `3` items appear where `11` are expected